# Scoped registries for GitHub packages
@decoperations:registry=https://npm.pkg.github.com
@dial-wtf:registry=https://npm.pkg.github.com

# Default registry for all other packages (must come after scoped registries)
registry=https://registry.npmjs.org/

# Auth token for GitHub registry
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
always-auth=true
